import 'package:flutter/material.dart';

class CalorieCalculatorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('i am calorie cal'),
    );
  }
}
