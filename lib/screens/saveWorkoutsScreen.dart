import 'package:flutter/material.dart';

class SaveWorkoutsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('save workouts'),
    );
  }
}
